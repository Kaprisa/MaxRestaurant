doctype html
html
	head
		meta(charset = 'utf-8')
		meta(name='viewport', content="initial-scale=1.0, width=device-width")
		link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
		block styles
			if name
				link(rel="stylesheet", href=`/dist/css/${name}.css`)
		title Max
	body
		include partials/header
		block content
		include partials/footer
		script(src=`https://maps.googleapis.com/maps/api/js?key=AIzaSyBHATj8Iaht38T3dEoL_WKqh7vBLGKiOC4&libraries=places`)
		block scripts
			if name
				//if name === 'contacts'
					script(src=`https://maps.googleapis.com/maps/api/js?key=${process.env.MAP_KEY}&libraries=places`)
				if (['index'].includes(name))
					script(src='/dist/js/common.js')
				script(src=`/dist/js/${name}.js`)